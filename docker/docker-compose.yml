services:
  redis:
    image: redis/redis-stack:latest
    ports:
      - 6379:6379
    healthcheck:
      test: [ "CMD", "redis-cli", "ping" ]
    volumes:
      - redis_data:/data



volumes:
  redis_data: